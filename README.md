##Day 1:
1. getting hands on experience in Typesript 
<!-- interface Student{
    id: number,
    name : string,
    enroll_No: number
};

function getInformation(id: number): Student{
    return{
        id:id,
        name:`student ${id}`,
        enroll_No: 2244
    }
}

console.log(getInformation(2));

// defining the object
let person: {
    name: string,
    age: number
};

person={
    name:'raj',
    age: 25,
}

console.log(person); -->

2. Created a simple express server.
